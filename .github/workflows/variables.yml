name: variable_usecase

on:
  workflow_dispatch:
  push:
    branches:
      - master
      - variables
env:
  name: Vysh

jobs:
  testing_variables:
    runs-on: ubuntu-latest
    steps:
      - name : Varible_check
        run : echo ""My name is $name" >> $GITHUB_STEP_SUMMARY
  
